mixin video(video)
    div
        h4 
            a(href="/videos/" + video.id)=info.title
        ul 
            li #{info.rating}/5
            li #{info.rating} comments.
            li Posted #{info.createdAt}
            li #{info.views} views.